<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CMS</title>
  <style>
    /* Reset and Base Styling */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    /* Navbar Styles */
    nav {
      background-color: #2c3e50;
      color: white;
      padding: 15px 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    nav .nav-left {
      font-size: 22px;
      font-weight: bold;
    }

    nav .nav-right a {
      color: white;
      text-decoration: none;
      font-size: 16px;
      background-color: #2980b9;
      padding: 8px 16px;
      border-radius: 5px;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      transition: background-color 0.3s ease;
    }

    nav .nav-right a:hover {
      background-color: #3498db;
    }

    nav .nav-right a .plus-icon {
      font-size: 18px;
    }
  .read-more {
  display: inline-block;
  margin-top: 10px;
  color: white;
  background-color: #2980b9;
  padding: 6px 12px;
  border-radius: 4px;
  text-decoration: none;
  font-size: 14px;
  transition: background-color 0.3s ease;
}

.read-more:hover {
  background-color: #3498db;
}


    /* Card Container */
   .container {
  padding: 40px 20px;
  display: grid;
  grid-template-columns: repeat(3, 1fr); /* Max 3 per row */
  gap: 20px;
  max-width: 1200px;
  margin: 0 auto;
}



    /* Card Styles */
    .card {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-bottom: 40px;
      transition: transform 0.2s ease;
    }

    .card:hover {
      transform: translateY(-5px);
    }

    .card h2 {
      font-size: 20px;
      color: #2c3e50;
      margin-bottom: 8px;
    }

    .card h4 {
      font-size: 16px;
      color: #7f8c8d;
      margin-bottom: 10px;
    }

    .card p {
      font-size: 14px;
      color: #555;
      line-height: 1.6;
    }
    @media (max-width: 900px) {
  .container {
    grid-template-columns: repeat(2, 1fr); /* 2 on tablets */
  }
}

@media (max-width: 600px) {
  .container {
    grid-template-columns: 1fr; /* 1 on mobile */
  }
}
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <div class="nav-left">CMS</div>
    <div class="nav-right">
      <a href="/createBlog">
        <span class="plus-icon">+</span>
        Create Blog
      </a>
    </div>
  </nav>

  <!-- Cards Section -->
  <div class="container">
  

  <% if (blogs.length === 0) { %>
  <p style="text-align:center; grid-column: 1/-1;">No blogs available.</p>
<% } else { %>
  <% blogs.forEach((blog) => { %>
    <div class="card">
      <h2><%- blog.title %></h2>
      <h4><%- blog.subTitle %></h4>
      <p><%- blog.description %></p>
       <a class="read-more" href="/single/<%= blog.id %>">Read More</a>
    </div>
  <% }) %>
<% } %>
</body>
</html>
