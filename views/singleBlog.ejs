<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Single Blog</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      background-color: #f4f4f4;
      color: #333;
    }

    /* Navbar */
    nav {
      background-color: #2c3e50;
      color: white;
      padding: 15px 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    nav .logo {
      font-size: 22px;
      font-weight: bold;
    }

    nav .back-link a {
      color: white;
      text-decoration: none;
      background-color: #2980b9;
      padding: 8px 16px;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    nav .back-link a:hover {
      background-color: #3498db;
    }

    /* Blog Content */
    .blog-container {
      max-width: 800px;
      margin: 40px auto;
      background-color: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .blog-container h1 {
      font-size: 32px;
      color: #2c3e50;
      margin-bottom: 10px;
    }

    .blog-container h3 {
      font-size: 18px;
      color: #7f8c8d;
      margin-bottom: 20px;
    }
      a{
         color: white;
      text-decoration: none;
    }

    .blog-container p {
      font-size: 16px;
      line-height: 1.7;
      color: #444;
    }

    .blog-container .date {
      font-size: 14px;
      color: #888;
      margin-top: 20px;
      font-style: italic;
    }
    .btn-delete {
  display: inline-block;
  padding: 8px 16px;
  background-color: #e74c3c;
  color: white;
  text-decoration: none;
  margin-top: 10px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}
.btn-delete:hover {
  background-color: #c0392b;
}

  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <div class="logo"><a href="/">CMS</a></div>
    <div class="back-link">
      <a href="/">← Back to Blogs</a>
    </div>
  </nav>

  <!-- Blog Content -->
  <div class="blog-container">
    <h1><%= singleBlogs[0].title %></h1>
    <h3><%= singleBlogs[0].subTitle %></h3>
    <p>
      <%= singleBlogs[0].description %></p>
    <p class="date">Published on <%- singleBlogs[0].createdAt.toLocaleDateString() %></p>
   <a href="/delete/<%-singleBlogs[0].id %>" class="btn-delete">Delete</a>

  </div>

</body>
</html>
